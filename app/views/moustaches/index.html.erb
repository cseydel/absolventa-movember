<h1>Listing moustaches</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moustaches.each do |moustach| %>
  <tr>
    <td><%= moustach.title %></td>
    <td><%= moustach.description %></td>
    <td><%= link_to 'Show', moustach %></td>
    <td><%= link_to 'Edit', edit_moustach_path(moustach) %></td>
    <td><%= link_to 'Destroy', moustach, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Moustache', new_moustach_path %>
