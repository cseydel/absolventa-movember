<%= form_for(@bro) do |f| %>
  <% if @bro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bro.errors.count, "error") %> prohibited this bro from being saved:</h2>

      <ul>
      <% @bro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :firstname %><br />
    <%= f.text_field :firstname %>
  </div>
  <div class="field">
    <%= f.label :lastname %><br />
    <%= f.text_area :lastname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
