<h1>Listing bros</h1>

<table>
  <tr>
    <th>Firstname</th>
    <th>Lastname</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bros.each do |bro| %>
  <tr>
    <td><%= bro.firstname %></td>
    <td><%= bro.lastname %></td>
    <td><%= link_to 'Show', bro %></td>
    <td><%= link_to 'Edit', edit_bro_path(bro) %></td>
    <td><%= link_to 'Destroy', bro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bro', new_bro_path %>
